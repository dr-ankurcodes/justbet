from setuptools import setup, find_packages

setup(
    name="justbet-sdk",
    version="0.1.0",
    description="Python SDK for just.bet (WINR Protocol) — built for AI agents",
    python_requires=">=3.9",
    packages=find_packages(),
    install_requires=[],  # zero external dependencies — pure stdlib
)
